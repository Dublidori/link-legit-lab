import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'link-legit-lab',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
